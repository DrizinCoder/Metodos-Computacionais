function I = simpson_38(f, a, b)
    % Na aplicação única, n é fixo em 3
    h = (b - a) / 3;
    
    % Calcula os dois pontos internos
    x1 = a + h;
    x2 = a + 2 * h;
    
    % Aplica a fórmula direta de 4 pontos
    I = (3 * h / 8) * (f(a) + 3 * f(x1) + 3 * f(x2) + f(b));
endfunction
